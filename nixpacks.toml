[phases.setup]
nixPkgs = ["php82", "php82Extensions.mysqlnd", "php82Extensions.pdo", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.openssl", "php82Extensions.json"]

[phases.install]
cmds = ["echo 'PHP dependencies installed'"]

[phases.build]
cmds = ["echo 'Building PHP app...'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."